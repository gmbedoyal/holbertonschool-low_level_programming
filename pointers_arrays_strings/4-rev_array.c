#include "main.h"
/**
 * reverse_array - check code
 * @*a: pointer
 * @n: integer
 * Return: void
*/

void reverse_array(int *a, int n)
{
int * p = a;
int i = 0;

while (*a)
	i++;

*p = a[n] - a[i];
}

