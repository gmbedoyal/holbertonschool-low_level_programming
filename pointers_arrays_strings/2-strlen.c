#include "main.h"
#include <string.h>
/**
 *_strelen - check the code
 *Description - longitud de un string
 *@s: pointer
 *Return: length of strenght
 */

int _strlen(char *s)
{
	int a;

	a = strlen(s);

return(a);
}
