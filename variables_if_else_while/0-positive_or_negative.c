#include <stdlib.h>
#include <time.h>
/**
 * main - descripción
 * return - 0 if 0, <0 if negative, >0 if positive
 **/i
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n<0) 
	return ("is negative")
	if (n = 0)
	return ("is zero")
	if (n > 0)
	return ("is positive");
}
