#include <stdlib.h>
#include <stdio.h>

/**
 * main - descripción de main
 *
 * Return: Always 0 (success)
 */


int main(void)
{
	int n = 0;
	char l = 'a';
	{
	while (n <= 15)
		printf("%d", n++);
	while (l <= 'f')
		printf("%c", l++);
	}
	printf("\n");
return (0);
}
