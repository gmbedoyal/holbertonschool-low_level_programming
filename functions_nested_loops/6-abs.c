#include "main.h"
#include <stdlib.h>
#include <ctype.h>

/**
 *_isabs - check the code
 *
 * @i: número a evaluar
 * Description - evaluate if alphabetic
 * Return: 0 always.
 */

int _abs(int)
{
	int i = abs (i);
	-putchar (i);

	return (0);
}
