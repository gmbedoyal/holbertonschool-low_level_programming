#include "main.h"
#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>

/**
 *_isabs - check the code
 *
 * @i: número a evaluar
 * Description - evaluate if alphabetic
 * Return: 0 always.
 */

int _abs(int i)
{
	int j = abs(i);

	if (j > 0)
	{	
	printf("%d\n", j);
	}
	else
	return (0);
}
