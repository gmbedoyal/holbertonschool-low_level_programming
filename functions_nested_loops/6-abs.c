#include "main.h"
#include <stdlib.h>
#include <ctype.h>

/**
 *_isabs - check the code
 *
 * @i: número a evaluar
 * Description - evaluate if alphabetic
 * Return: 0 always.
 */

int _abs(int i)
{
	int j;

	if (abs(i) > 0)
	{	
	_putchar(j);
	}
	else
	return (0);
}
