#include "main.h"
#include <stdlib.h>
#include <ctype.h>

/**
 * int_islower - check the code
 *
 * Return: 0 if uppercase, 1 if lowercase.
 */

int _islower(int c);
{
	int c;
	int d = islower (c);

	if (d !== 0) /*si la letra es minúscula*/
	{

		return (1);
	}
	else 
		return (0);
}


