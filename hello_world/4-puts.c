#include <stdio.h>
#include <string.h>
/**
 * main - Descripción de la función main
 * str1 - Descripción del string
 */
int main(void)
{
char str1[55];

strcpy(str1, "\"Programming is like building a multilingual puzzle");

puts(str1);
/**
 * Return: must return 0
 */
return (0);
}
